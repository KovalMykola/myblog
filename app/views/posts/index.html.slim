h2 Listing posts

- @posts.each do |post|
  h3 = post.title
  p = post.content
  div
    b Author:
    = post.user.email 
	 
    p
    '
    = link_to 'Show', post, class: 'btn btn-info btn-xs' 
    ' 
    = link_to 'Edit', edit_post_path(post), class: 'btn btn-warning btn-xs' 
    '
    = link_to 'Destroy', post, class: 'btn btn-danger btn-xs', method: :delete, data: { confirm: 'Are you sure?' }

p  
= link_to 'New Post', new_post_path, class: 'btn btn-primary' 
